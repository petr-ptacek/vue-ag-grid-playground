/**
 * @typedef {import('ag-grid-community').GridOptions} GridOptions
 */

/**
 * @param {GridOptions} options
 * @returns {GridOptions}
 */
export function defineGridOptions(options) {
  return options;
}